
  # Trip Planner App

  This is a code bundle for Trip Planner App. The original project is available at https://www.figma.com/design/81X4GBzosAhSdcNpSi3pxG/Trip-Planner-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  